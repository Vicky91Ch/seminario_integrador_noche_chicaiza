// Sin tipo — TypeScript lo infiere automáticamente del valor
const nombre3 = "Ana";   // TypeScript sabe que es string
const edad3   = 28;      // TypeScript sabe que es number
const activo3 = true;    // TypeScript sabe que es boolean

console.log(`Me llamo ${nombre3}`);
console.log(`Tengo ${edad3} años.`);
console.log(`Vivo en Madrid.`);
